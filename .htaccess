<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Route all public storage requests through the public directory, 
    # bypassing the physical root storage folder
    RewriteRule ^storage/(.*)$ public/storage/$1 [L]
    
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>